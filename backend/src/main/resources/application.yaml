spring:
  datasource:
    url: jdbc:h2:mem:spadb
    username: sa
    password: palabrapaso
    driverClassName: org.h2.Driver
  jpa:
    spring.jpa.database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none
  h2:
    console.enabled: true